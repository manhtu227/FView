# Copy to .env for CLI, or set AI_API_KEY in local.properties for the sample app (debug only).
# Use any OpenAI-compatible chat completions provider (set AI_BASE_URL / AI_MODEL if needed).
AI_API_KEY=
# AI_BASE_URL=https://api.x.ai/v1
# AI_MODEL=grok-4.5
